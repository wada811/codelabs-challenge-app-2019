apply from: "$rootDir/gradle/script/library.gradle"

repositories {
    maven { url "https://jitpack.io" }
}

dependencies {
    api 'com.github.wada811.LifecycleDisposable:lifecycledisposable:1.0.0'
}
